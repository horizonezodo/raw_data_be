spring:
  application:
    name: demo

  datasource:
    url: jdbc:oracle:thin:@113.190.234.241:1521/dbwr
    username: EPAS_UAT_BUS_COREBANK
    password: 123456
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://10.10.0.37:8012/realms/EPAS
          jwk-set-uri: http://10.10.0.37:8012/realms/EPAS/protocol/openid-connect/certs

bpm:
  engine:
    rest:
      enabled: true
  core:
    cors:
      allowed-origins:
        - http://localhost:4303
        - http://localhost:8303
        - http://localhost:4306
        - https://10.10.0.37:4306
        - https://epas.ngvgroup.vn
      allowed-methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      allowed-headers:
        - "*"
      exposed-headers:
        - Authorization
      allow-credentials: true
      max-age: 3600
    oauth2:
      token-uri: http://10.10.0.37:8012/realms/EPAS/protocol/openid-connect/token
      client-id: epas_service
      client-secret: GTw6sLKCUQdbE6Jq1CHCxDiXpq7Mr7A5
      cache-token: true
    web:
      ignoring-paths:
        - /api-docs/**
        - /swagger-ui.html
        - /swagger-ui/**
        - /h2-console/**
        - /actuator/**
    stored-procedure:
      enabled: true
      connection-pool:
        initial-size: 5
        max-size: 20
        min-idle: 5
        connection-timeout: 30000
        idle-timeout: 600000
        max-lifetime: 1800000
      timeout:
        connection: 30000
        query: 60000
      retry:
        enabled: true
        max-attempts: 3
        backoff-delay: 1000
        
# Cấu hình Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    tryItOutEnabled: true
    filter: true
  packages-to-scan:  com.ngvgroup.bpm.engine, org.camunda.bpm.engine.rest,com.ngvgroup.bpm.beaiagent



server:
  port: 8208
  servlet:
    context-path: /api
    encoding:
      charset: UTF-8
      enabled: true

service:
  common:
    url: http://10.10.0.37:8201
  bus-pcf:
    url: http://10.10.0.37:8203
