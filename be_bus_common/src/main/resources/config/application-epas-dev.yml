spring:
  application:
    name: bus-common-service

  datasource:
    url: jdbc:oracle:thin:@113.190.234.241:31521/ORCLPDB1
    username: EPAS_DEV_BUSINESS
    password: 123456
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://10.10.0.31:31090/realms/EPAS_DEV
          jwk-set-uri: http://10.10.0.31:31090/realms/EPAS_DEV/protocol/openid-connect/certs

bpm:
  engine:
    rest:
      enabled: true
  core:
    cors:
      allowed-origins:
        - https://epas.dev.ngvgroup.vn,
        - https://demo.dev.ngvgroup.vn,
    oauth2:
      token-uri: http://10.10.0.31:31090/realms/EPAS_DEV/protocol/openid-connect/token
      client-id: epas_service
      client-secret: 30EiPJ7G5O9DWc8YroWViOWETiPxB29f
      cache-token: true
    stored-procedure:
      enabled: true
      connection-pool:
        initial-size: 5
        max-size: 20
        min-idle: 5
        connection-timeout: 30000
        idle-timeout: 600000
        max-lifetime: 1800000
      timeout:
        connection: 30000
        query: 60000
      retry:
        enabled: true
        max-attempts: 3
        backoff-delay: 1000
server:
  port: 8080
  servlet:
    context-path: /api
    encoding:
      charset: UTF-8
      enabled: true

minio:
  url: http://10.10.0.31:9200
  access-key: admin
  secret-key: admin123
  bucket: epas

service:
  common:
    url: https://epas.dev.ngvgroup.vn/api-common
  bus-pcf:
    url: https://epas.dev.ngvgroup.vn/api-bus-pcf