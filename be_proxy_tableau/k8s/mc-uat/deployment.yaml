apiVersion: apps/v1
kind: Deployment
metadata:
  name: tableau-proxy-be
  namespace: mc-uat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tableau-proxy-be
      environment: mc-uat
  template:
    metadata:
      labels:
        app: tableau-proxy-be
        environment: mc-uat
    spec:
      containers:
        - name: tableau-proxy-be
          image: localhost:32000/tableau-proxy:latest
          ports:
            - containerPort: 80
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: mc-uat
      terminationGracePeriodSeconds: 60

---
apiVersion: v1
kind: Service
metadata:
  name: tableau-proxy-be-service
  namespace: mc-uat
spec:
  selector:
    app: tableau-proxy-be
    environment: mc-uat
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tableau-proxy-be-ingress
  namespace: mc-uat
spec:
  ingressClassName: public
  rules:
    - host: tableau-proxy-mc.uat.ngvgroup.vn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tableau-proxy-be-service
                port:
                  number: 80
